import pkg from '@prisma/client'; const { PrismaClient } = pkg; const prisma = new PrismaClient(); async function main() { const gurus = await prisma.user.findMany({ where: { role: 'GURU' }, include: { halaqoh: true } }); console.log(JSON.stringify(gurus.map(g => ({ name: g.nama, halaqohs: g.halaqoh.map(h => ({ name: h.nama, kategori: h.kategori })) })), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());